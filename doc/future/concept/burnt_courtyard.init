# `l_rasilis.init` includes a group label, which `wolves.init` uses because
# it's the most recent label given.


include characters/l_party.init    # Has label.

include characters/l_rasilis.init  # Has label.
include monsters/wolves.init       # Does not have label.
